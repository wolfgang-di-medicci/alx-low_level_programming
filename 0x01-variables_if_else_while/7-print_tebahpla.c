#include<stdio.h>

/**
 * main - main entry point
 * Return: 0 when succesful
*/

int main(void)
{
	char reva;

	for (reva = 'z'; reva >= 'a'; reva--)
	{
		putchar(reva);
	}
	putchar('\n')
		return (0);
}
